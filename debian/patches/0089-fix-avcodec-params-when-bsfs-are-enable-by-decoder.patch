Index: jellyfin-ffmpeg/libavcodec/decode.c
===================================================================
--- jellyfin-ffmpeg.orig/libavcodec/decode.c
+++ jellyfin-ffmpeg/libavcodec/decode.c
@@ -212,6 +212,9 @@ static int decode_bsfs_init(AVCodecConte
     ret = av_bsf_init(avci->bsf);
     if (ret < 0)
         goto fail;
+    ret = avcodec_parameters_to_context(avctx, avci->bsf->par_out);
+    if (ret < 0)
+        goto fail;
 
     return 0;
 fail:
